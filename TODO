Update README
Clean up various names of everything (muxmed, medmux, docker container, tmux session, etc. etc. etc.)
set up .emacs in docker?
upgrade tintin?
requirements for this package (docker, tmux, etc.) vs for the docker container itself (zlib, pcre, etc.)
requirements also: med font and iterm profile